import type { TypingFontSize } from "@/types/setting/setting.types";

export const TYPING_FONT_SIZE_CLASS: Record<TypingFontSize, string> = {
  small: "text-xs",
  medium: "text-base",
  large: "text-xl",
};
